name: github.com/opspec-pkgs/git.commit
description: commits to a git repo
inputs:
  repo:
    dir:
      description: repo to commit to
      default: .git
  authorName:
    string:
      description: commit authors name
  authorEmail:
    string:
      description: commit authors email address
      constraints: { format: email }
  message:
    string:
      description: details about the commit
  noVerify:
    string:
      description: whether to skip pre-commit and commit-msg hooks
      constraints: { enum: ['true', 'false']}
      default: 'false'
outputs:
  repo:
    dir:
      description: repo with the commit
      default: .git
version: 1.0.0
run:
  container:
    cmd: [ /cmd.sh ]
    dirs:
      /.git: $(repo)
    envVars:
      authorEmail:
      authorName:
      message:
      noVerify:
    files:
      /cmd.sh:
    image: { ref: 'opspecpkgs/git.commit:1.0.0' }
    workDir: /
